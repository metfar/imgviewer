# imgviewer
This is a fast, keyboard-first image viewer written in Python with pygame. Its interface is inspired by zgv: filenames and directories stay visible in a list on the left while the selected image is displayed on the right.
